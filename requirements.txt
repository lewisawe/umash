# The deterministic product (CLI, policy layer, session loop, tests) needs NO
# third-party packages — it runs on the Python standard library alone.
#
# These deps are ONLY for the conversational Strands agent on Bedrock
# (umash/agent.py, `python demo.py` without --offline). Install them with:
#     pip install -r requirements.txt
# or, equivalently, the packaging extra:
#     pip install -e ".[agent]"
strands-agents==1.53.0
boto3>=1.35.0
